# Changelog

## Unreleased

- Creación actual de la base documental: `AGENTS.md`, `CLAUDE.md` y estructura `docs/`.
- Preparación de guías para agentes, contexto, arquitectura, decisiones, roadmap, analytics, investigación, tareas, reportes y archivo.

## Historial confirmado

- `476e595` - SEO de producción e instrumentación de analytics/ads. Cambió `analytics-config.js`, páginas HTML, `generar_paises.py`, `robots.txt`, `script.js` y `sitemap.xml`.
- `8f76a83` - Add campaign playbook for the smoke test. Agregó `ADS.md` y actualizó `README.md`.
- `ba7caf9` - Capture first-touch UTM attribution and attach it to every lead. Actualizó `script.js`.
- `81638ba` - Add brand favicon, social share cards and 404 page. Agregó `404.html`, favicon, apple touch icon y social share cards en `assets/og/`.
- `6e4b09e` - Apply Vercel Web Interface Guidelines audit fixes. Actualizó HTML, `generar_paises.py`, `script.js` y `styles.css`.
- `179f368` - Add accessibility layer, purchase-intent copy and fix template extensions. Actualizó HTML, `generar_paises.py` y `styles.css`.
- `f8b1b5c` - Add SEO layer: sitemap, canonical URLs and structured data. Agregó `sitemap.xml`, `robots.txt` y metadata en páginas HTML.
